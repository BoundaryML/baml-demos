[project]
name = "baml-deep-research-demo"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["baml_core", "pydantic>=2", "streamlit>=1.40"]

[tool.uv]
package = false

[tool.uv.sources]
baml_core = { path = "/Users/sam/baml/baml_language/sdks/python", editable = true }
